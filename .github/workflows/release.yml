name: Build and Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven

      - name: Get version from tag
        id: version
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          POM_VERSION="$(mvn help:evaluate -Dexpression=project.version -q -DforceStdout)"
          test "$VERSION" = "$POM_VERSION"
          echo "VERSION=$VERSION" >> "$GITHUB_OUTPUT"

      - name: Test live-check tooling offline
        run: python3 -m unittest scripts/test_live_compatibility.py

      - name: Build and test fat JAR
        run: mvn -B clean package

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: target/AIProxyOauth-${{ steps.version.outputs.VERSION }}.jar
